# Лендинг натяжных потолков со сметой и уточняющим квизом

Демо-лендинг вымышленной бригады: расчёт цены прямо на странице — построчная
смета, пять уточнений, итог вилкой и печать сметы отдельным документом.

Шестая из семи новых работ для портфолио профиля FL.ru `kusaka63`.

## С чего начать

Читать `docs/superpowers/specs/2026-09-22-potolki-smeta-design.md` — там
решение целиком: предмет, что работа показывает, устройство расчёта,
структура страницы, граница по персональным данным и что проверяется.

Дальше по порядку: план работ, сборка, предпросмотр со скриншотом, правки,
выкладка на GitHub Pages.

## Состояние на 25.09.2026

Работа собрана: статические секции, калькулятор с построчной сметой и
итогом-вилкой, печать сметы отдельным документом с датой и оговоркой про
замер, форма-демонстрация, которая ничего не отправляет, и подрезанный,
вшитый в страницу шрифт Inter — страница не делает ни одного внешнего
запроса.

Проверяется командой `npm test` (27 тестов расчёта на встроенном тестовом
механизме Node, без зависимостей) и командой `npm run check` (десять
проверок в браузере: подсветка строки при уточнении, печатный режим,
360 px, контраст, чистая консоль, ноль внешних запросов). Скриншоты —
командой `npm run shot`, результат в `tools/shots/`.

Замер Lighthouse 13.4.1, три прогона на `http://localhost:8080/`, медиана
из `zamery/lh-1.json` — `zamery/lh-3.json`: производительность 100,
доступность 100, лучшие практики 100, SEO 63, LCP 1,4 с.

SEO ниже остальных категорий — это осознанное решение, а не недоработка.
Единственная проваленная проверка Lighthouse в этой категории —
`is-crawlable`, потому что на странице стоит
`<meta name="robots" content="noindex, nofollow">`. Компания вымышленная,
цены условные, странице незачем попадать в выдачу — индексация закрыта
нарочно.

Живого адреса пока нет: выкладка на GitHub Pages ещё не сделана.

## Чем отличается от работы на другом профиле

На `serhio2490` есть шаблон B2B-лендинга под четыре ниши, где калькулятор и
квиз заменяют друг друга. Здесь одна настоящая ниша, а квиз уточняет смету —
каждое уточнение сразу двигает итог и подсвечивает строку, на которую
повлияло.

## Четыре решения в расчёте

Смета построчная — видно, из чего сложился итог. Итог вилкой, а не точной
цифрой: точную даёт замер. Результат показывается без телефона, а не в обмен
на контакт. И калькулятор сам называет, чего он не знает: состояние стен,
высота, нестандартные углы, закладные под люстру.

## Границы

**Отзывов на странице нет.** Выдуманный отзыв в портфолио читается как
выдуманный отзыв. Вместо них — разбор одной сметы.

**Форма не отправляет и не собирает.** Комплект документов и ответ на вопрос,
куда падают заявки, — это четвёртая работа портфолио, `klinika-152fz`, и со
страницы стоит ссылка на неё.

**Без React.** Почти вся страница статическая, состояние — десяток полей
расчёта. Расчёт вынесен чистой функцией и покрыт тестами на встроенном
механизме Node, без зависимостей из npm.

## Выкладка

GitHub Pages, рабочий аккаунт `pyhphhddb8-eng`, репозиторий `potolki-smeta`.
Страница закрыта от поисковиков: компания вымышленная, цены условные.
